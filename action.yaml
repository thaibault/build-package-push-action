name: Build package and push

inputs:
  debug:
    description: Sets "NODE_ENV" to "debug".
    required: false
    default: false
  type:
    description: Sets semantic version update type.
    default: patch
    type: choice
    options:
      - major
      - minor
      - patch

runs:
  using: composite

  steps:
    - uses: thaibault/install-npm-package-dependencies-action@main
      with:
        debug: ${{ inputs.debug }}

    - name: Get the version determining script.
      run: |
        mkdir --parents .git/build-package-and-push-action && \
        curl \
          --output '.git/build-package-and-push-action/determine-version.sh' \
          'https://raw.githubusercontent.com/thaibault/build-package-push-action/refs/heads/main/determine-version.sh'
      shell: bash

    - name: Update package.json mentioned version.
      id: version
      # NOTE: Declare variable beforehand to avoid shadowing the nested shell's
      # exit code.
      run: |
        NEW_VERSION="$(bash .git/build-package-and-push-action/determine-version.sh --format 'v${MAJOR}.${MINOR}.${PATH}${CANDIDATE}' ${{inputs.type || 'patch'}})" \
        echo Determined new version \"${NEW_VERSION}\". \
        echo "new=${NEW_VERSION}" >> "$GITHUB_OUTPUT"
      shell: bash

    - name: print
      run: echo '${{steps.version.new}}'
      shell: bash

    #- name: Create git tag for newly create package version.
      #run: git tag "${{outputs.NEW_VERSION}}"
      #shell: bash

    #- name: Update artefacts in git repository.
      #run: git commit --all --message 'Publish new version ${{outputs.NEW_VERSION}}.' && git push --tags && git push
      #shell: bash

    #- name: Publish newly created package.
      #env:
      #  NODE_ENV: ${{ contains(inputs.debug, 'true') && 'debug' || '' }}
      #run: yarn publish
